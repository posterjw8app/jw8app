{
  "name": "jw8app",
  "private": true,
  "version": "1.0.0",
  "description": "零依赖五子棋静态网站",
  "scripts": {
    "build": "node build.js",
    "deploy": "npm run build && npx wrangler pages deploy dist --project-name jw8app",
    "deploy:pages": "npm run deploy"
  }
}
